---
inclusion: always
---

# 法考笔记规范(自动加载)

本仓库的笔记生成规范见下方引入的 `CLAUDE.md`。

⚠️ **分科规范文件不会自动加载。** 处理任何一科前必须显式读取 `00_规范/<部门法>.md`,
并在开始前声明读到了哪些文件。读不到就中止询问,不要用通用规则代跑。

⚠️ 幻觉率较高的模型请同时读 `00_样本/反例/反例_典型幻觉形态.md`。

可复制的分步指令见 `00_流程/指令卡.md`。

#[[file:CLAUDE.md]]
