# 临时文件（agent 写的统计脚本、暂存片段等，一律 _tmp_ 前缀）
_tmp_*
*.tmp

# 运行日志
.log_*.txt
**/.log_*.txt

# Obsidian 本地状态
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache

# 系统文件
.DS_Store
Thumbs.db

# ⚠️ 素材（00_素材/**/*.txt）刻意**不**排除，随仓库一起版本控制。
#
# 理由:
# 1. 纯文本体积很小(八科合计约 8 MB,实测),GitHub 完全放得下
# 2. 素材不可再生(重跑 ASR 成本高),而模型持有 write 权限时存在误改风险。
#    入库后任何改动都能一眼看出并还原,不必手工复制备份
# 3. 换电脑 clone 一次就齐了
